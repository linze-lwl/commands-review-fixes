# 在函数中如何访问其他服务？

函数计算只提供一个执行环境，在函数中访问其他服务例如OSS、MNS、数据库、自建的HTTP服务等，或者访问函数计算（例如调用触发一个函数），与您在本地访问这些服务是没有差异的。您只需将相应的依赖库跟函数代码一起打包上传，就可以在函数中访问相应的服务。关于打包依赖的具体操作，请参见[为函数安装第三方依赖](https://help.aliyun.com/zh/functioncompute/fc/user-guide/install-third-party-dependencies-for-a-function)。
