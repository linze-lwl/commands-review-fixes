# 「不兼容」概览

E2B 文档中包含 MCP gateway，用于在沙箱中运行或接入 MCP Server。云沙箱当前不兼容 E2B MCP gateway 能力。

不要在云沙箱功能说明中引导开发者使用以下能力：

- E2B MCP gateway Quickstart。
- E2B Available servers。
- E2B Custom templates。
- E2B Custom servers。

如果业务需要 MCP 能力，应在应用层或独立服务中集成 MCP Server，再通过云沙箱执行受控命令或访问明确的 API。

