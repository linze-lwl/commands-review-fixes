# 「不兼容」生命周期事件

E2B 文档中包含 Lifecycle Events API 和 Webhooks。云沙箱当前未提供与 E2B 完全兼容的生命周期事件订阅能力。

不建议将以下设计作为云沙箱当前接入路径：

- 依赖沙箱生命周期 Webhook 推动业务状态机。
- 依赖 E2B Lifecycle Events API 做事件回放。

如果需要跟踪任务状态，应在业务系统中围绕 `sandboxId`、命令执行结果和应用日志建立状态流转。

