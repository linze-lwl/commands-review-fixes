# 「不兼容」快照

E2B 支持通过 Snapshot 保存沙箱状态，并用 Snapshot 创建新的沙箱。云沙箱当前不兼容 E2B Snapshot API。

不要在云沙箱接入路径中使用以下能力：

- `sandbox.createSnapshot()`
- `sandbox.listSnapshots()`
- `Sandbox.create(snapshotId)`

如果业务需要保留文件或任务结果，应使用 Filesystem 读写、上传下载 URL、OSS、NAS 或业务数据库完成持久化。

