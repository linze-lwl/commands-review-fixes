# 创建沙箱

使用 CLI 可以快速创建一个云沙箱，适合本地验证模板、命令和网络访问。

```bash
e2b sandbox create
```

如果需要指定模板，请使用 CLI 当前版本支持的模板参数。不同 CLI 版本参数可能存在差异，执行前可查看帮助：

```bash
e2b sandbox create --help
```

创建后请保存输出中的沙箱 ID，后续可用于终止沙箱或排查问题。

