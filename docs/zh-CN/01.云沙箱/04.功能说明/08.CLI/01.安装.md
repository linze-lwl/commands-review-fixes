# 安装

E2B CLI 可用于在命令行中创建、查看和终止云沙箱，也可用于查看模板列表。

## 安装 CLI

```bash
npm install -g e2b
```

安装后确认版本：

```bash
e2b --version
```

## 配置连接参数

```bash
export E2B_API_KEY="<your-api-key>"
export E2B_ACCESS_TOKEN=$E2B_API_KEY
export E2B_API_URL="https://api.<region>.e2b.fc.aliyuncs.com"
export E2B_DOMAIN="<region>.e2b.fc.aliyuncs.com"
```

CLI 与 SDK 使用同一套云沙箱访问参数。`E2B_ACCESS_TOKEN` 用于兼容 E2B CLI 的认证读取方式。

