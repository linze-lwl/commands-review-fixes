# 查看模板

云沙箱扩展了部分 Template 管理命令。可以使用 CLI 查看当前账号下可用模板：

```bash
e2b template list
```

E2B 官方 CLI 文档当前主要展开 Sandbox 相关命令，未单独提供 `template list` 页面。接入云沙箱时，请以本地 CLI 帮助和云沙箱兼容说明为准：

```bash
e2b template --help
e2b template list --help
```

模板列表适合用于确认模板名称、模板 ID 和构建结果。创建生产沙箱时，建议使用已经完成冒烟验证的模板。
