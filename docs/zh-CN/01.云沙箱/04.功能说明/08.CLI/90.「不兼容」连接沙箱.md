# 「不兼容」连接沙箱

E2B CLI 文档中包含连接沙箱的命令。云沙箱当前兼容说明未将 CLI 连接沙箱列为明确兼容能力。

需要连接已有沙箱时，请使用 SDK：

```typescript
const sandbox = await Sandbox.connect("<sandbox-id>", {
  apiKey: process.env.E2B_API_KEY,
  apiUrl: process.env.E2B_API_URL,
  domain: process.env.E2B_DOMAIN,
});
```

