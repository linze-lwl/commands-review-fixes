# 「不兼容」执行命令

E2B CLI 文档中包含在沙箱内执行命令的能力。云沙箱当前兼容说明未将 CLI 执行命令列为明确兼容能力。

需要在沙箱中执行命令时，请使用 SDK 的 Commands：

```typescript
const result = await sandbox.commands.run("echo hello");
console.log(result.stdout.trim());
```

