# 「不兼容」Amp

E2B Amp 文档基于 E2B 预构建 `amp` 模板，示例包括 `e2b sbx create amp`、`Sandbox.create('amp')`、运行 Amp CLI、JSON streaming、线程管理以及从 `amp` 模板继续构建自定义模板。

云沙箱当前不兼容 E2B `amp` 预构建模板和对应集成教程。

如果需要在云沙箱中运行 Amp，应先自行构建包含 Amp CLI 的自定义模板，并由业务侧管理 `AMP_API_KEY`、仓库凭证、命令执行和结果采集。

