# 「不兼容」概览

E2B Volume API 当前尚未兼容云沙箱。

## 当前状态

以下能力暂不作为云沙箱 E2B 兼容能力使用：

- `Volume.create/list/delete`
- `volume.readFile/writeFile/listDir/stat`
- `Sandbox.create({ volumeMounts })`

## 推荐方案

如果需要持久化存储或云上数据访问，建议优先使用 NAS、OSS 等云存储方案。

## 兼容状态

Volume 兼容能力正式可用前，不建议在云沙箱文档中引导开发者调用 E2B Volume API。
