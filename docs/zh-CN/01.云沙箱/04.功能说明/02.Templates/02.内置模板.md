# 内置模板

内置模板用于快速创建 Sandbox。快速入门和代码解释器示例默认使用 `code-interpreter-v1`。

## 查看模板

使用 CLI 查看当前账号可用模板：

```bash
export E2B_API_KEY="<your-api-key>"
export E2B_ACCESS_TOKEN=$E2B_API_KEY
export E2B_API_URL="https://api.<region>.e2b.fc.aliyuncs.com"
export E2B_DOMAIN="<region>.e2b.fc.aliyuncs.com"

e2b template list
```

## 使用模板创建 Sandbox

Python 示例：

```python
import os

sandbox = Sandbox.create(
    template="code-interpreter-v1",
    api_key=os.environ["E2B_API_KEY"],
    api_url=os.environ["E2B_API_URL"],
    domain=os.environ["E2B_DOMAIN"],
)
```

TypeScript 示例：

```typescript
const sandbox = await Sandbox.create("code-interpreter-v1", {
  apiKey: process.env.E2B_API_KEY,
  apiUrl: process.env.E2B_API_URL,
  domain: process.env.E2B_DOMAIN,
});
```

## 后续阅读

- 需要预装依赖或工具链时，参见[自定义模板](03.自定义模板.md)。
- 需要查看模板标签、别名和构建状态时，参见[管理模板](15.管理模板.md)。
