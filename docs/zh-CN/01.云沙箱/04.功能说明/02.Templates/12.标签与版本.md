# 标签与版本

标签用于给模板增加可读的版本或环境标记，例如 `prod`、`staging`、`v1`。适合做灰度、回滚和模板分组。

## 分配标签

```typescript
import { Template } from "e2b";

await Template.assignTags("agent-python313-20260704", ["staging", "v1"], {
  apiKey: process.env.E2B_API_KEY,
  apiUrl: process.env.E2B_API_URL,
  domain: process.env.E2B_DOMAIN,
});
```

## 查询标签

```typescript
const tags = await Template.getTags("agent-python313-20260704", {
  apiKey: process.env.E2B_API_KEY,
  apiUrl: process.env.E2B_API_URL,
  domain: process.env.E2B_DOMAIN,
});

console.log(tags);
```

## 移除标签

```typescript
await Template.removeTags("agent-python313-20260704", "staging", {
  apiKey: process.env.E2B_API_KEY,
  apiUrl: process.env.E2B_API_URL,
  domain: process.env.E2B_DOMAIN,
});
```

## 使用建议

标签适合表达“阶段”和“版本”，模板名称适合表达“运行环境”。不要把复杂发布规则完全塞进标签，生产切换仍应由业务系统或发布系统控制。

