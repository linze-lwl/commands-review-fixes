# 「不兼容」监听变化

E2B Filesystem 支持监听目录变化。云沙箱当前环境实测 `sandbox.files.watchDir()` 返回未实现错误，不应作为正式兼容能力引导开发者使用。

不要在云沙箱生产逻辑中依赖以下能力：

- `sandbox.files.watchDir()`
- 基于文件变化事件驱动任务状态机
- 基于 watch 事件等待 Agent 输出文件

## 替代方式

如果只是等待单个文件出现，建议使用 `sandbox.files.exists()` 做有限次数轮询；如果需要处理任务状态，建议由业务系统记录状态，而不是依赖沙箱文件事件。
